query {
    accountList{
        id,
        name,
        expenses,
        incomes,
        initialAmount,
        isDefault,
        currencyId,
        currencyCode,
        currencyName,
        currencySymbol,
    }
}
