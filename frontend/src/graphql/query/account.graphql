query Account($id: Int!){
    account(id: $id){
        id,
        name,
        expenses,
        incomes,
        initialAmount,
        isDefault,
        currencyId,
        currencyCode,
        currencyName,
        currencySymbol,
    }
}
